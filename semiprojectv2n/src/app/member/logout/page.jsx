'use client'

import React from "react";

const Login = () => {
    // localStorage.clear();
    localStorage.removeItem('accessToken');

    return (
        location.href = "/"
    );
}

export default Login;
